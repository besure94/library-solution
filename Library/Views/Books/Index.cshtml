@{
  Layout = "_Layout";
}

@using Library.Models;
@model List<Library.Models.Book>;

<h1>Books</h1>

@if (@Model.Count == 0)
{
  <h3>No books have been added yet.</h3>
}
else
{
  <ul>
    @foreach (Book book in Model)
    {
      <li>@Html.ActionLink($"{book.Title}", "Details", new { id = book.BookId })</li>
    }
  </ul>
}

<p>@Html.ActionLink("Create new book", "Create")</p>
<p>@Html.ActionLink("View overdue books", "OverdueBooks")</p>
<p>@Html.ActionLink("Home", "Index", "Home")</p>